{
    "binary": 0,
    "timeout": 10,
    "seed": 10000223,
    "params": {
        "max_size": 1000,
        "expr": "A*B+C*D-E*F+G*H-I*J"
    },
    "description": "complex matrix operations 2 (smaller than ${matrixsize} * ${matrixsize}, ${timeout} sec timeout)",
    "preparation": "cat ${filename} > /dev/null"
}
