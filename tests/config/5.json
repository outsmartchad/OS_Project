{
    "binary": 0,
    "timeout": 1,
    "seed": 10000139,
    "params": {
        "max_size": 500,
        "expr": "A*B*C*D"
    },
    "description": "consecutive matrix multiplications (smaller than ${matrixsize} * ${matrixsize}, ${timeout} sec timeout)",
    "preparation": "cat ${filename} > /dev/null"
}
